<% if profile.uploaded_image.persisted? %>
    <%= image_tag(profile.uploaded_image) %>
<% else %>
    <%= image_tag(profile.img_url, class: "rounded-full border-2 border-gray-300 shadow-lg w-16 h-16") %>
<% end %>